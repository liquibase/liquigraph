# Liquigraph integration with Spring Boot

This is an example of a simplistic integration of Liquigraph (3.x) with Spring Boot.

# Run

```shell
 $> mvn spring-boot:run
```

Then proceed to `http://localhost:8080` and see the result of the migration!
